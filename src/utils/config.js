// For Firebase JS SDK v7.20.0 and later, measurementId is optional

//add firebase configuration and export it here

//image configuration
export const imageConfig = {
  quality: 0.2,
  maxWidth: 800,
  maxHeight: 600,
  autoRotate: true,
};
